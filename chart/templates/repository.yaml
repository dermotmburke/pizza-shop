apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-repository
  labels:
    {{- include "pizza-shop.labels" . | nindent 4 }}
    app.kubernetes.io/component: repository
spec:
  replicas: {{ .Values.repository.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Chart.Name }}
      app.kubernetes.io/component: repository
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Chart.Name }}
        app.kubernetes.io/component: repository
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: repository
          image: {{ .Values.image.registry }}/{{ .Values.image.repository }}/pizza-shop-repository:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.repository.port }}
          env:
            {{- include "pizza-shop.appEnv" . | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ .Values.repository.port }}
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ .Values.repository.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-repository
  labels:
    {{- include "pizza-shop.labels" . | nindent 4 }}
    app.kubernetes.io/component: repository
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/component: repository
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.repository.port }}
      targetPort: {{ .Values.repository.port }}
